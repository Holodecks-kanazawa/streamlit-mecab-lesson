{
  "name": "Streamlit MeCab App",
  "image": "mcr.microsoft.com/devcontainers/python:3.9",
  "features": {
    "ghcr.io/devcontainers/features/git:1": {}
  },
  "forwardPorts": [8501],
  "portsAttributes": {
    "8501": {
      "label": "Streamlit App",
      "onAutoForward": "notify"
    }
  },
  "postCreateCommand": "sudo apt-get update && sudo apt-get install -y mecab libmecab-dev mecab-ipadic-utf8 && pip install -r requirements.txt && wget https://moji.or.jp/wp-content/ipafont/IPAexfont/ipaexg00401.zip && unzip -o ipaexg00401.zip && rm -f ipaexg00401.zip"
}
